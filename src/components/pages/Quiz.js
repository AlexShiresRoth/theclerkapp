import React from 'react';
import Layout from '../layout';
import QuizHeader from '../quiz/QuizHeader';
const Quiz = () => {
	return (
		<Layout>
			<QuizHeader />
		</Layout>
	);
};

export default Quiz;
